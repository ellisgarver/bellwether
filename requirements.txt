# =============================================================================
# Macro Narrative Dynamics — Pinned Dependencies
# =============================================================================
# Versions pinned for reproducibility (see plan §8.7).
# Update this file whenever any dependency is changed and re-commit.
#
# Tested under Python >=3.11,<3.13.
# =============================================================================

# Core scientific stack ------------------------------------------------------
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1
scikit-learn==1.5.2

# Configuration & I/O -------------------------------------------------------
pyyaml==6.0.2
pyarrow==17.0.0           # parquet backend
python-dotenv==1.0.1
pydantic==2.9.2

# Embedding & transformers --------------------------------------------------
# NOTE: torch should be installed per-platform. See README install section
# for CUDA / ROCm / CPU instructions. The version below is the CPU baseline.
torch==2.4.1
transformers==4.51.3
sentence-transformers==3.2.1
accelerate==1.0.1

# Clustering ----------------------------------------------------------------
bertopic==0.16.4
umap-learn==0.5.6
hdbscan==0.8.38.post2
nltk==3.9.1

# Bayesian dynamics ---------------------------------------------------------
pymc==5.16.2
arviz==0.19.0

# Deduplication & text ------------------------------------------------------
datasketch==1.6.5         # MinHash near-duplicate detection
mmh3==5.0.1               # Fast hashing
trafilatura==1.12.2       # Article text extraction from raw HTML
tiktoken==0.7.0           # BPE tokenizer for 600-token document chunking
pypdf==5.0.1              # PDF text extraction (CEA Economic Report of the President via govinfo); ADR-020

# Data ingestion ------------------------------------------------------------
fredapi==0.5.2
requests==2.32.3
httpx==0.27.2
curl_cffi==0.15.0          # Chrome TLS+HTTP/2 impersonation for Akamai-protected sites (IMF); ADR-014
feedparser==6.0.11
beautifulsoup4==4.12.3
lxml==5.3.0
lxml_html_clean==0.4.1    # Required by trafilatura/justext with lxml>=5.0 (lxml.html.clean split out)
tenacity==9.0.0           # Retry logic for flaky scraping

# Layer 1B journalism dynamics is now Media Cloud Premium (ADR-016).
# The prior RavenPack-via-WRDS plan was abandoned; no WRDS dependencies needed.

# Dashboard -----------------------------------------------------------------
streamlit==1.39.0
plotly==5.24.1

# Validation / stats --------------------------------------------------------
statsmodels==0.14.4

# Workflow / orchestration --------------------------------------------------
tqdm==4.66.6
click==8.1.7              # CLI for scripts/

# Development / tests (also see requirements-dev.txt) -----------------------
pytest==8.3.3
